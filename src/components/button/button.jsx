import React from "react";

const Button = () => {
    return (
        <>
            <button className="btn btn-primary" type="button">
                Agregar
            </button>
        </>
    );
};

export default Button;
